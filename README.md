# ✈️ Agência de Viagens - Landing Page

Esta é uma página inicial (`landing page`) para uma agência de viagens, focada em uma estrutura clara e responsiva para apresentar destinos e pacotes.

## 🧱 Componentes Semânticos

O projeto utiliza a seguinte estrutura para melhor organização e acessibilidade:

* **`<header>`/`<nav>`:** Navegação e logo.
* **`.hero` (`<section>`):** Banner principal com chamada para ação (CTA).
* **`.destaques` (`<section>`):** Grid de cartões (`.card`) para destacar os destinos mais populares.
* **`<footer>`:** Informações de contato e direitos autorais.

## 📱 Responsividade

O layout utiliza `flexbox` e Media Queries para garantir que o conteúdo se ajuste bem tanto em telas de desktop quanto em dispositivos móveis.
